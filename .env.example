# PostgreSQL credentials for local development only.
# Copy this file to .env before starting the container.
POSTGRES_USER=devuser
POSTGRES_PASSWORD=devpassword
POSTGRES_DB=devdb

# Host port exposed by Docker. Change it if 5432 is already in use.
POSTGRES_PORT=5432
